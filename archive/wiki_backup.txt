from discord.ext import commands
import discord
from discord import app_commands


class Wiki(commands.Cog):
    def __init__(self, bot):
        self.bot = bot

    @app_commands.command(name="wiki", description="Search the SoT Wiki")
    @app_commands.choices(search=[ # Das hier sind die Presets
        app_commands.Choice(name="Commendations", value="Commendations")], page_anchor=[
        app_commands.Choice(name="Cast Iron Merchant", value="Cast_Iron_Merchant")])
    async def report(self, interaction: discord.Interaction, search: app_commands.Choice[str], page_anchor: app_commands.Choice[str]):
        #await interaction.response.send_message(f"Du hast user {user.mention} ausgewählt. 
        #Dein String ist **{reason}**. Du hast diese option ausgewählt: {option} (Option kann mit if statement abgefragt werden)", ephemeral=True)
        await interaction.response.send_message(f"https://seaofthieves.fandom.com/wiki/{search}#{page_anchor}")
        """
        if search.value == "blubb":
            print("blubb")
        elif search.value == "jay":
            print("Jay")
        """
async def setup(bot):
    await bot.add_cog(Wiki(bot))